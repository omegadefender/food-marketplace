<div class=”ui segment”>

<div class=”ui segment”>
 <h3>Create a New Conversation View</h3>
  <div class=”ui list”>
   <% @users.each do |user| %>
    <% if user.id != current_user.id %>
     <div class=”item”>
   <%= user.firstname %> <%= link_to('Message this person', conversations_path(sender_id: current_user.id, recipient_id: user.id), method: 'post')%>
 </div>
  <% end %>
 <% end %>
 </div>
</div>